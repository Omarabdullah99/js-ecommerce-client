import Products from "../../Products/Products";
const RelatedProducts = () => {
    return (
        <div className="related-products">
        <Products headingText="Releted Products" />
        </div>
    );
};

export default RelatedProducts;
